# Phase 10 - Task 01 Spec (Methodology Page + ADR 006 + i18n Namespace)

## Inputs and mandatory references

- Architecture input: `plans/phase-10-arch.md`
- Agent protocol: `docs/agent-protocol.md` (Tatico must provide detailed implementation spec plus edge-case tests)
- Project conventions: `.github/copilot-instructions.md`
- Wireframe reference: `docs/wireframes/WF-09-methodology.html` (open in browser before implementing)
- Design tokens: `docs/adr/000-wireframes.md` (CSS custom properties, typography, navigation)
- i18n structure: `site/src/locales/pt-BR/common.json`, `site/src/locales/en-US/common.json`
- Existing component: `site/src/components/MethodologyBadge.jsx` (links to `/metodologia`)
- Existing placeholder: `site/src/pages/MethodologyPage.jsx` (to be replaced)

## 1) Files to create or modify (exact relative paths)

1. `site/src/locales/pt-BR/methodology.json` — **CREATE** — pt-BR i18n namespace
2. `site/src/locales/en-US/methodology.json` — **CREATE** — en-US i18n namespace
3. `site/src/main.jsx` — **MODIFY** — register `methodology` i18n namespace
4. `site/src/pages/MethodologyPage.jsx` — **MODIFY** — replace placeholder with full implementation
5. `site/src/styles.css` — **MODIFY** — add methodology page CSS classes
6. `docs/adr/006-transparency-methodology.md` — **CREATE** — ADR 006

No new dependencies are introduced. All imports come from existing packages (`react`, `react-i18next`, `react-helmet-async`, `react-router-dom`).

## 2) Function signatures and types per file

### 2.1 `site/src/locales/pt-BR/methodology.json`

Exact JSON structure (all keys mandatory):

```json
{
  "title": "Metodologia",
  "subtitle": "Como funciona o Portal Eleicoes BR 2026",
  "disclaimer": {
    "heading": "Declaracao de Independencia",
    "body": "Este portal e um projeto independente, sem filiacao partidaria ou financiamento eleitoral. Nao ha intervencao editorial de nenhuma forma: os artigos sao coletados automaticamente de fontes publicas, os resumos sao gerados por inteligencia artificial, e as analises de sentimento sao derivadas algoritmicamente. O codigo-fonte completo esta disponivel para consulta e auditoria publica em github.com/carlosduplar/eleicoes-2026-monitor."
  },
  "pipeline": {
    "heading": "Como funciona o pipeline",
    "collection": {
      "label": "Coleta",
      "body": "RSS de 20 veiculos + scraping de sites partidarios e institutos de pesquisa, a cada 10 minutos via GitHub Actions."
    },
    "summarization": {
      "label": "Sumarizacao",
      "body": "Modelos de linguagem open-source com fallback hierarquico: NVIDIA NIM (gratuito) -> OpenRouter (gratuito, 200 req/dia) -> Ollama Cloud (gratuito) -> Vertex AI Gemini 2.5 Flash Lite (pago prioritario) -> MiMo V2 Flash."
    },
    "sentiment": {
      "label": "Sentimento",
      "body": "Analise algoritmica do tom das noticias coletadas. Os scores refletem o tom das manchetes e resumos -- nao uma avaliacao editorial ou pesquisa de opiniao."
    },
    "polls": {
      "label": "Pesquisas Eleitorais",
      "body": "Dados coletados automaticamente dos sites dos institutos. Reproduzidos sem modificacao editorial."
    },
    "quiz": {
      "label": "Quiz de Afinidade",
      "body": "Posicoes extraidas de declaracoes verificadas nas noticias coletadas. Fontes exibidas no resultado -- nunca durante as perguntas."
    }
  },
  "limitations": {
    "heading": "Limitacoes conhecidas",
    "items": [
      "Modelos de IA podem cometer erros de interpretacao.",
      "A cobertura de fontes pode ter vies de disponibilidade -- veiculos sem RSS nao sao incluidos.",
      "Posicoes de candidatos evoluem ao longo da campanha; o pipeline atualiza diariamente.",
      "Analise de sentimento nao equivale a pesquisa de opiniao."
    ]
  },
  "error_reporting": {
    "heading": "Reportar erros",
    "body": "Encontrou um erro? Abra uma issue no repositorio publico.",
    "cta": "Abrir issue no GitHub"
  },
  "repo_link": "https://github.com/carlosduplar/eleicoes-2026-monitor"
}
```

**IMPORTANT**: Use the EXACT accented Portuguese text from `plans/phase-10-arch.md` lines 19-65, not the ASCII approximation shown above. The architecture spec contains the authoritative content with proper diacritics (e.g., "Declaracao" -> "Declaração", "nao" -> "não", etc.).

### 2.2 `site/src/locales/en-US/methodology.json`

```json
{
  "title": "Methodology",
  "subtitle": "How the Brazil Elections 2026 Portal Works",
  "disclaimer": {
    "heading": "Independence Statement",
    "body": "This portal is an independent project with no party affiliation or electoral funding. There is no editorial intervention of any kind: articles are collected automatically from public sources, summaries are generated by artificial intelligence, and sentiment analyses are derived algorithmically. The complete source code is available for public consultation and audit at github.com/carlosduplar/eleicoes-2026-monitor."
  },
  "pipeline": {
    "heading": "How the pipeline works",
    "collection": {
      "label": "Collection",
      "body": "RSS from 20+ outlets and scraping of party websites and polling institutes, every 10 minutes via GitHub Actions."
    },
    "summarization": {
      "label": "Summarization",
      "body": "Open-source language models with hierarchical fallback: NVIDIA NIM (free) -> OpenRouter (free, 200 req/day) -> Ollama Cloud (free) -> Vertex AI Gemini 2.5 Flash Lite (paid priority) -> MiMo V2 Flash."
    },
    "sentiment": {
      "label": "Sentiment",
      "body": "Algorithmic analysis of news tone. Scores reflect the tone of headlines and summaries -- not an editorial evaluation or opinion poll."
    },
    "polls": {
      "label": "Election Polls",
      "body": "Automatically collected from polling institute websites. Reproduced without editorial modification."
    },
    "quiz": {
      "label": "Affinity Quiz",
      "body": "Positions extracted from verified statements in collected news. Sources shown in results only -- never during questions."
    }
  },
  "limitations": {
    "heading": "Known limitations",
    "items": [
      "AI models can make interpretation errors.",
      "Source coverage may have availability bias -- outlets without RSS are not included.",
      "Candidate positions evolve during the campaign; the pipeline updates daily.",
      "Sentiment analysis is not equivalent to opinion polling."
    ]
  },
  "error_reporting": {
    "heading": "Report errors",
    "body": "Found an error? Open an issue on the public repository.",
    "cta": "Open issue on GitHub"
  },
  "repo_link": "https://github.com/carlosduplar/eleicoes-2026-monitor"
}
```

### 2.3 `site/src/main.jsx` — Changes

Add methodology namespace imports and register them in i18n resources.

**Current state (lines 7-8):**
```jsx
import enCommon from './locales/en-US/common.json';
import ptCommon from './locales/pt-BR/common.json';
```

**Target state:**
```jsx
import enCommon from './locales/en-US/common.json';
import enMethodology from './locales/en-US/methodology.json';
import ptCommon from './locales/pt-BR/common.json';
import ptMethodology from './locales/pt-BR/methodology.json';
```

**Current state (lines 26-28):**
```jsx
    resources: {
      'pt-BR': { common: ptCommon },
      'en-US': { common: enCommon },
    },
```

**Target state:**
```jsx
    resources: {
      'pt-BR': { common: ptCommon, methodology: ptMethodology },
      'en-US': { common: enCommon, methodology: enMethodology },
    },
```

**Current state (line 34):**
```jsx
    ns: ['common'],
```

**Target state:**
```jsx
    ns: ['common', 'methodology'],
```

### 2.4 `site/src/pages/MethodologyPage.jsx` — Full replacement

Replace the entire placeholder file. The component must:

**Imports:**
```jsx
import * as ReactHelmetAsync from 'react-helmet-async';
import { useTranslation } from 'react-i18next';
```

**Component signature:**
```jsx
function MethodologyPage(): JSX.Element
```

**Internal structure:**
- `useTranslation('methodology')` for the `methodology` namespace
- `useTranslation('common')` for the brand name in Helmet title
- `<Helmet>` with `<title>` and JSON-LD `<script type="application/ld+json">`

**Sections rendered (in order):**
1. **Header**: `<h1>` with `t('title')` + `<p>` subtitle with `t('subtitle')`
2. **Disclaimer**: `<section>` with CSS class `methodology-disclaimer` — highlighted box
3. **Pipeline**: `<section>` with ordered steps (collection, summarization, sentiment, polls, quiz) — each step numbered 1-5
4. **Limitations**: `<section>` with `<ul>` iterating `t('limitations.items', { returnObjects: true })`
5. **Error reporting**: `<section>` with `<a>` linking to GitHub issues
6. **Repo link**: `<p>` with link to repository

**Pipeline steps array** (defined inside the component or as a constant):
```jsx
const PIPELINE_STEPS = ['collection', 'summarization', 'sentiment', 'polls', 'quiz'];
```

Each step renders:
```jsx
<div className="methodology-step">
  <div className="methodology-step-num">{index + 1}</div>
  <div>
    <h3>{t(`pipeline.${step}.label`)}</h3>
    <p>{t(`pipeline.${step}.body`)}</p>
  </div>
</div>
```

**Limitations rendering:**
```jsx
const limitationItems = t('limitations.items', { returnObjects: true });
// limitationItems is an array of strings
// Render as <li> elements inside <ul>
```

**GitHub issues link:**
```jsx
<a
  href="https://github.com/carlosduplar/eleicoes-2026-monitor/issues"
  target="_blank"
  rel="noopener noreferrer"
>
  {t('error_reporting.cta')}
</a>
```

**Repo link at bottom:**
```jsx
<a
  href={t('repo_link')}
  target="_blank"
  rel="noopener noreferrer"
>
  {t('repo_link')}
</a>
```

**Helmet content:**
```jsx
<Helmet>
  <title>{`${t('title')} | ${tCommon('brand')}`}</title>
  <script type="application/ld+json">
    {JSON.stringify({
      "@context": "https://schema.org",
      "@type": ["AboutPage", "FAQPage"],
      "name": "Metodologia — Portal Eleicoes BR 2026",
      "description": "Como funciona o pipeline de coleta e analise do portal.",
      "url": "https://eleicoes2026.com.br/metodologia"
    })}
  </script>
</Helmet>
```

**Note on Helmet import pattern** — must match the existing pattern used in `App.jsx` (line 14):
```jsx
const Helmet = ReactHelmetAsync.Helmet || ReactHelmetAsync.default?.Helmet;
```

### 2.5 `site/src/styles.css` — Additions

Append the following CSS classes to the end of the file (before the `@media` queries at line 598). These classes follow the WF-09 wireframe patterns:

```css
/* --- Methodology Page (WF-09) --- */

.methodology-page {
  max-width: 860px;
  margin: 0 auto;
  padding-bottom: 2rem;
}

.methodology-page h1 {
  font-size: clamp(1.5rem, 1.2rem + 0.8vw, 1.8rem);
  margin-bottom: 0.35rem;
}

.methodology-subtitle {
  color: var(--text-secondary);
  font-size: 1rem;
  margin-bottom: 1.5rem;
}

.methodology-section {
  margin-bottom: 2rem;
}

.methodology-section h2 {
  font-size: 1.25rem;
  font-weight: 600;
  font-family: Georgia, 'Times New Roman', serif;
  margin-bottom: 1rem;
}

.methodology-disclaimer {
  border-left: 4px solid var(--brand-navy);
  background: var(--brand-muted);
  padding: 1rem 1.25rem;
  border-radius: 0 8px 8px 0;
  margin-bottom: 2rem;
}

.methodology-disclaimer p {
  color: var(--text-secondary);
  font-size: 0.9rem;
  line-height: 1.7;
}

.methodology-steps {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.methodology-step {
  display: flex;
  gap: 1rem;
  background: var(--brand-surface);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 1rem 1.25rem;
}

.methodology-step-num {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background: var(--brand-navy);
  color: #ffffff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.875rem;
  font-weight: 700;
  flex-shrink: 0;
}

.methodology-step h3 {
  font-size: 0.875rem;
  font-weight: 600;
  margin-bottom: 0.25rem;
}

.methodology-step p {
  color: var(--text-secondary);
  font-size: 0.875rem;
}

.methodology-limitations {
  list-style: none;
  padding: 0;
  margin: 0;
}

.methodology-limitations li {
  color: var(--text-secondary);
  font-size: 0.875rem;
  padding: 0.35rem 0;
  display: flex;
  align-items: flex-start;
  gap: 0.5rem;
}

.methodology-limitations li::before {
  content: '\26A0\FE0F';
  flex-shrink: 0;
}

.methodology-error-section {
  text-align: center;
  padding: 1.5rem 0;
}

.methodology-error-section h2 {
  text-align: center;
}

.methodology-error-section p {
  color: var(--text-secondary);
  font-size: 0.9rem;
  margin-bottom: 0.75rem;
}

.methodology-cta {
  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
  background: var(--brand-navy);
  color: #ffffff;
  text-decoration: none;
  font-size: 0.875rem;
  font-weight: 500;
  padding: 0.6rem 1.25rem;
  border-radius: 6px;
}

.methodology-cta:hover {
  opacity: 0.9;
}

.methodology-repo {
  text-align: center;
  padding: 1rem 0;
  font-size: 0.85rem;
  color: var(--text-secondary);
}

.methodology-repo a {
  color: var(--brand-gold);
  text-decoration: none;
  font-weight: 500;
}

.methodology-repo a:hover {
  text-decoration: underline;
}
```

**Mobile responsive additions** — append inside the `@media (max-width: 900px)` block:

```css
  .methodology-page {
    max-width: 100%;
  }
```

### 2.6 `docs/adr/006-transparency-methodology.md` — CREATE

Use the EXACT content from `plans/phase-10-arch.md` lines 113-129:

```markdown
# ADR 006 — Transparency and Methodology Page

## Status
Accepted

## Context
This portal uses AI to generate summaries and sentiment scores on electoral news. Without transparency, users cannot distinguish AI analysis from editorial opinion. The GDPR-adjacent principle of explainability applies, even for public non-personal data.

## Decision
A dedicated /metodologia page is mandatory. MethodologyBadge links to it from every data-driven component. The page discloses: independence, pipeline steps, AI providers, known limitations, and an error reporting channel.

## Consequences
- Users can audit methodology and report errors
- MethodologyBadge acts as a trust signal on all dashboards
- ADR is living documentation — update when new AI providers or sources are added
```

## 3) Data contract notes (schema fields each file must satisfy)

This phase does **not** consume data from `data/` JSON files or `docs/schemas/*.schema.json`. The Methodology page is a static editorial page whose content comes entirely from i18n locale files.

### Contracts this phase DOES satisfy:

| Constraint | Source | File affected |
|------------|--------|---------------|
| No hardcoded strings in JSX | copilot-instructions.md | `MethodologyPage.jsx` — all text via `useTranslation('methodology')` |
| `methodology` i18n namespace with `pt-BR` + `en-US` | phase-10-arch.md | `methodology.json` in both locales |
| CSS custom properties from wireframes | ADR 000 | `styles.css` — uses `--brand-navy`, `--brand-muted`, `--brand-surface`, `--border`, `--text-secondary`, `--brand-gold`, `--text-primary` |
| `MethodologyBadge` links to `/metodologia` | copilot-instructions.md, Phase 07 | Route already registered in `App.jsx` line 70 — page must render without error at this route |
| GitHub link opens in new tab | phase-10-arch.md constraints | `<a target="_blank" rel="noopener noreferrer">` |
| JSON-LD AboutPage + FAQPage | phase-10-arch.md Section 3 | `<Helmet>` in `MethodologyPage.jsx` |

### i18n key contract

Both `pt-BR/methodology.json` and `en-US/methodology.json` must have identical key structures:

```
title                          -> string
subtitle                       -> string
disclaimer.heading             -> string
disclaimer.body                -> string
pipeline.heading               -> string
pipeline.collection.label      -> string
pipeline.collection.body       -> string
pipeline.summarization.label   -> string
pipeline.summarization.body    -> string
pipeline.sentiment.label       -> string
pipeline.sentiment.body        -> string
pipeline.polls.label           -> string
pipeline.polls.body            -> string
pipeline.quiz.label            -> string
pipeline.quiz.body             -> string
limitations.heading            -> string
limitations.items              -> string[] (array of 4 strings)
error_reporting.heading        -> string
error_reporting.body           -> string
error_reporting.cta            -> string
repo_link                      -> string (URL)
```

## 4) Step-by-step implementation order (dependency-aware)

1. **Read references**: Open `docs/wireframes/WF-09-methodology.html` in browser. Study the visual layout, spacing, and section structure.

2. **Create `site/src/locales/pt-BR/methodology.json`**: Copy the exact content from `plans/phase-10-arch.md` lines 19-65. Ensure all Portuguese diacritics (ã, ç, é, ê, ó, etc.) are preserved.

3. **Create `site/src/locales/en-US/methodology.json`**: Write English equivalents for all keys. Use the translations specified in `plans/phase-10-arch.md` lines 68-78.

4. **Modify `site/src/main.jsx`**: Add imports for the new methodology namespace files and register them in the i18n `resources` and `ns` config. Three changes total (imports, resources object, ns array).

5. **Add methodology CSS to `site/src/styles.css`**: Insert new CSS classes before the `@media` queries. Add mobile override inside the existing `@media (max-width: 900px)` block.

6. **Replace `site/src/pages/MethodologyPage.jsx`**: Remove the placeholder and implement the full component with 5 sections, Helmet, JSON-LD, and all i18n integration.

7. **Create `docs/adr/006-transparency-methodology.md`**: Copy exact ADR content from the architecture spec.

8. **Verify build succeeds**:
   ```powershell
   Push-Location site; npm run build; Pop-Location
   ```

9. **Verify MethodologyBadge link works**: Check that `/metodologia` renders all 5 sections (manual browser inspection or dev server check).

10. **Create sentinel**: `New-Item -Path plans/phase-10-arch.DONE -ItemType File -Force`

11. **Commit** with message from Section 6.

**Dependency graph:**

```
Step 2 (pt-BR JSON)  ──┐
Step 3 (en-US JSON)  ──┤
                        ├── Step 4 (main.jsx) ── Step 6 (MethodologyPage.jsx) ── Step 8 (build)
Step 5 (styles.css)  ──┘
Step 7 (ADR) ────────── (independent, can be done in parallel with steps 2-6)
```

## 5) Exact PowerShell 7 commands to run tests and verify correctness

Run from repository root (`C:\projects\eleicoes-2026-monitor`):

```powershell
# 1. Verify locale files exist and are valid JSON
python -c "
import json
for lang in ['pt-BR', 'en-US']:
    path = f'site/src/locales/{lang}/methodology.json'
    with open(path, encoding='utf-8') as f:
        data = json.load(f)
    assert 'title' in data, f'{path}: missing title'
    assert 'subtitle' in data, f'{path}: missing subtitle'
    assert 'disclaimer' in data and 'heading' in data['disclaimer'], f'{path}: missing disclaimer.heading'
    assert 'pipeline' in data and 'heading' in data['pipeline'], f'{path}: missing pipeline.heading'
    for step in ['collection', 'summarization', 'sentiment', 'polls', 'quiz']:
        assert step in data['pipeline'], f'{path}: missing pipeline.{step}'
        assert 'label' in data['pipeline'][step], f'{path}: missing pipeline.{step}.label'
        assert 'body' in data['pipeline'][step], f'{path}: missing pipeline.{step}.body'
    assert 'limitations' in data and 'items' in data['limitations'], f'{path}: missing limitations.items'
    assert isinstance(data['limitations']['items'], list), f'{path}: limitations.items must be array'
    assert len(data['limitations']['items']) == 4, f'{path}: limitations.items must have 4 items'
    assert 'error_reporting' in data and 'cta' in data['error_reporting'], f'{path}: missing error_reporting.cta'
    assert 'repo_link' in data, f'{path}: missing repo_link'
    print(f'{path}: valid ({len(data)} top-level keys)')
"

# 2. Verify both locale files have identical key structures
python -c "
import json
def get_keys(obj, prefix=''):
    keys = set()
    for k, v in obj.items():
        full = f'{prefix}.{k}' if prefix else k
        if isinstance(v, dict):
            keys.update(get_keys(v, full))
        else:
            keys.add(full)
    return keys

with open('site/src/locales/pt-BR/methodology.json', encoding='utf-8') as f:
    pt = json.load(f)
with open('site/src/locales/en-US/methodology.json', encoding='utf-8') as f:
    en = json.load(f)

pt_keys = get_keys(pt)
en_keys = get_keys(en)
if pt_keys != en_keys:
    missing_en = pt_keys - en_keys
    missing_pt = en_keys - pt_keys
    raise AssertionError(f'Key mismatch. Missing in en-US: {missing_en}. Missing in pt-BR: {missing_pt}')
print(f'Both locale files have identical key structures ({len(pt_keys)} leaf keys)')
"

# 3. Verify main.jsx loads methodology namespace
$mainContent = Get-Content -Path site/src/main.jsx -Raw
if ($mainContent -notmatch "methodology") { throw "main.jsx does not reference methodology namespace" }
if ($mainContent -notmatch "ns:\s*\[.*'methodology'") { throw "main.jsx ns array does not include 'methodology'" }
Write-Host "main.jsx methodology namespace registration verified."

# 4. Verify MethodologyPage.jsx uses methodology namespace (not common placeholder)
$pageSrc = Get-Content -Path site/src/pages/MethodologyPage.jsx -Raw
if ($pageSrc -match "placeholder-page") { throw "MethodologyPage.jsx still has placeholder-page class" }
if ($pageSrc -match "placeholder\.phase_10") { throw "MethodologyPage.jsx still references placeholder.phase_10" }
if ($pageSrc -notmatch "useTranslation\(\s*['""]methodology['""]") { throw "MethodologyPage.jsx does not use methodology namespace" }
if ($pageSrc -notmatch "application/ld\+json") { throw "MethodologyPage.jsx missing JSON-LD script" }
if ($pageSrc -notmatch "AboutPage") { throw "MethodologyPage.jsx missing AboutPage in JSON-LD" }
if ($pageSrc -notmatch "FAQPage") { throw "MethodologyPage.jsx missing FAQPage in JSON-LD" }
if ($pageSrc -notmatch 'target="_blank"') { throw "MethodologyPage.jsx missing target=_blank on external links" }
if ($pageSrc -notmatch 'rel="noopener noreferrer"') { throw "MethodologyPage.jsx missing rel=noopener noreferrer" }
Write-Host "MethodologyPage.jsx structure verified."

# 5. Verify styles.css has methodology classes
$css = Get-Content -Path site/src/styles.css -Raw
if ($css -notmatch "\.methodology-page") { throw "styles.css missing .methodology-page class" }
if ($css -notmatch "\.methodology-disclaimer") { throw "styles.css missing .methodology-disclaimer class" }
if ($css -notmatch "\.methodology-step") { throw "styles.css missing .methodology-step class" }
if ($css -notmatch "\.methodology-limitations") { throw "styles.css missing .methodology-limitations class" }
if ($css -notmatch "\.methodology-cta") { throw "styles.css missing .methodology-cta class" }
Write-Host "styles.css methodology classes verified."

# 6. Verify ADR 006 exists
if (-not (Test-Path -Path docs/adr/006-transparency-methodology.md)) { throw "ADR 006 not created" }
$adr = Get-Content -Path docs/adr/006-transparency-methodology.md -Raw
if ($adr -notmatch "Accepted") { throw "ADR 006 missing 'Accepted' status" }
if ($adr -notmatch "MethodologyBadge") { throw "ADR 006 missing MethodologyBadge reference" }
Write-Host "ADR 006 verified."

# 7. Verify npm build succeeds
Push-Location site
npm run build
Pop-Location

# 8. Verify /metodologia route was generated in SSG output
if (-not (Test-Path -Path site/dist/metodologia/index.html)) {
  # Some SSG setups may not generate subdirectory -- check dist directly
  if (-not (Test-Path -Path site/dist/metodologia.html)) {
    Write-Host "WARNING: SSG output for /metodologia not found as static file. Verify manually via dev server."
  }
}

# 9. Verify no hardcoded Portuguese/English text in MethodologyPage.jsx (i18n compliance)
python -c "
import re
with open('site/src/pages/MethodologyPage.jsx', encoding='utf-8') as f:
    content = f.read()
# Check there are no raw Portuguese strings (common words) outside of i18n calls
# Allow: className strings, HTML attributes, schema.org URLs, import paths
lines = content.split('\n')
violations = []
for i, line in enumerate(lines, 1):
    stripped = line.strip()
    # Skip imports, comments, className, href, JSON-LD content
    if stripped.startswith('import') or stripped.startswith('//') or stripped.startswith('*'):
        continue
    # Check for common Portuguese/English content words that should be in i18n
    for word in ['Metodologia', 'Methodology', 'pipeline', 'Limitac', 'Known limitation']:
        if word in stripped and 't(' not in stripped and 'JSON.stringify' not in stripped and 'className' not in stripped:
            violations.append(f'  Line {i}: possible hardcoded text: {stripped[:80]}')
if violations:
    print('Potential i18n violations (review manually):')
    for v in violations:
        print(v)
else:
    print('No obvious i18n violations found.')
"
```

## 6) Git commit message to use (Conventional Commits + trailer)

```text
feat(phase-10): Methodology page + ADR 006 + i18n methodology namespace

- Implement MethodologyPage.jsx with 5 sections (disclaimer, pipeline,
  limitations, error reporting, repo link) following WF-09 wireframe
- Create pt-BR and en-US methodology.json locale files
- Register methodology i18n namespace in main.jsx
- Add methodology page CSS classes to styles.css
- Create ADR 006 (Transparency and Methodology Page)
- JSON-LD AboutPage + FAQPage schema in page head

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
```

## 7) Exact PowerShell command to create architecture completion sentinel

```powershell
New-Item -Path plans/phase-10-arch.DONE -ItemType File -Force
```

## Edge-case test scenarios (required by Tatico responsibilities)

1. **Language toggle mid-page**: User is on `/metodologia` in pt-BR, clicks EN toggle. All 5 sections must switch to English without page reload. Verify `limitations.items` array re-renders with English strings.

2. **Direct URL access in en-US**: User navigates directly to `/metodologia` with `localStorage lang = en-US`. Page must render fully in English on first paint (SSG hydration must not flash Portuguese).

3. **JSON-LD in SSG output**: The `<script type="application/ld+json">` must be present in the SSG-generated HTML (not just client-rendered). Verify in `site/dist/metodologia/index.html`.

4. **MethodologyBadge navigation**: Click `MethodologyBadge` on `/sentimento` page. Must navigate to `/metodologia` via React Router (client-side navigation, no full page reload).

5. **External link behavior**: The GitHub issues link (`error_reporting.cta`) must open in a new tab (`target="_blank"`). The `rel="noopener noreferrer"` attribute must be present for security.

6. **Mobile responsive**: At viewport 390px, the methodology page must not overflow horizontally. The `.methodology-page` max-width must adapt. Pipeline step cards must remain readable.

7. **Empty/missing namespace graceful degradation**: If methodology.json fails to load (network error in dev), `useTranslation` should return keys as fallback strings (i18n default behavior). Page must not crash.

8. **Concurrent namespace loading**: The `methodology` namespace loads alongside `common`. Verify that adding it to the `ns` array in main.jsx does not break existing pages that only use `common`.

9. **Disclaimer visual prominence**: The disclaimer section must NOT be plain text. It must have a visually distinct background (`var(--brand-muted)`) and left border (`4px solid var(--brand-navy)`) as specified in the architecture.

10. **Accessibility**: All sections should have semantic HTML (`<section>`, `<h1>`, `<h2>`, `<ul>`, `<li>`). The limitations list should use `<ul>` with `<li>` elements, not `<div>` wrappers.

11. **`repo_link` is a URL**: The `repo_link` value in both locale files must be a valid URL string (`https://github.com/carlosduplar/eleicoes-2026-monitor`), not a display-only label.
